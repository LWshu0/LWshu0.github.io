---
title: 计算机网络与因特网
copyright: true
date: 2023-08-27 23:39:39
updated: 2023-09-04 23:14:45
tags: 
    - 计算机网络
    - 课程笔记
categories:
    - 计算机网络
    - 课程学习笔记
excerpt: 计算机网络教程学习第一章内容的笔记, 供日后复习查看
index_img: /img/计算机网络与因特网/1693225990154.png
#banner_img:
---
# 因特网

- 其一，我们能够描述因特网的**具体构成**，即构成因特网的基本硬件和软件组件
- 其二，我们能够根据为分布式应用提供**服务**的联网基础设施来描述因特网

## 具体构成

因特网是一个遍及全世界计算设备(称为主机(host)或者端系统(end system))的计算机网络.

端系统通过通信链路(communication link)和分组交换机(packet switch)连接到一起

- 通信链路的**传输速率**以比特/秒(bit/s或者bps)度量
- 端到端传输信息需要将数据分段, 称为**分组(packet)**
- 分组交换机两种最著名的类型是 **路由器(router)** 和 **链路层交换机(link-layer switch)**
- 一个分组所经历的一系列通信链路和分组交换机称为通过该网络的 **路径 (route 或 path )**
- 端系统, 分组交换机和其他因特网部件都要运行一系列**协议 (protocol)**,这些协议控制因特网中信息的接收和发送, 主要有 **TCP(Transmission Control Protocol传输控制协议)** 和 **IP(Internet Protocol, 网际协议)**

## 服务描述

- 涉及多个相互交换数据的端系统的应用程序，被称为**分布式应用程序 (distributed application)**
- 两个端系统上的软件通过 **套接字接口(socket interface)** 进行数据通讯

## 协议

> **协议 (protocol)** 定义了在两个或多个通信实体之间交换的报文的格式和顺序，以及报文发送和／或接收一条报文或其他事件所采取的动作

## 网络结构

### 网络边缘

端系统也成为主机(host), 分为客户(client)和服务器(server), 其中客户通常指PC, 智能手机等, 服务器通常是更为强大的机器

![1693405710460](/img/计算机网络与因特网/1693405710460.png)

### 接入网和物理媒介

指将端系统物理连接到其边缘路由器的网络, 边缘路由是端系统到其他任何远程端系统的路径上的第一台路由器

![1693405749273](/img/计算机网络与因特网/1693405749273.png)

#### 接入网

**家庭接入**

如今流行的两种方式: **数字用户线(igital Subscriber Line, DSL)** 和电缆

![数字用户线](/img/计算机网络与因特网/1693225990154.png)

家庭的 DSL调制解调器得到数字数据后将其转换为高频音，以通过电话线传输给本地中心局；来自许多家庭的模拟信号在 DSLAM 处被转换回数字形式

家庭电话线同时承载了数据和传统的电话信号，它们用不同的频率进行编码:

- 高速下行信道，位于 50kHz 到 1MHz 频段
- 中速上行信道，位于 4kHz 到 50kHz 频段
- 普通的双向电话信道，位千 0 到 4kHz 频段

这种方法使单根 DSL 线路看起来就像有 3 根单独的线路一样， 因此一个电话呼叫和一个因特网连接能够同时共享 DSL 链路(**频分复用**)

**电缆因特网接入 (cable Internet access )** 利用了有线电视公司现有的有线电视基础设施, 如下图为 **混合光纤同轴 ( Hybrid Fiber Coax , HFC) 系统**

![混合光纤同轴](/img/计算机网络与因特网/1693226393116.png)

> DSLAM 和 CMTS 是**不对称的**, 即上行速率和下行速率不同, 通常下行速率要高, 因为相比较起来我们一般是接收数据多而发送数据少

**企业(和家庭)接入**

使用局域网(LAN)技术将端系统连接到边缘路由器, 以太网是目前最流行的接入技术

![以太网](/img/计算机网络与因特网/1693227033480.png)

**广域无线接入**

WiFi, LTE, 3G, 4G等

![1693406033274](/img/计算机网络与因特网/1693406033274.png)

#### 物理媒体

物理媒体可分为:

- 导引型媒体: 光缆, 双绞铜线等
- 非导引型媒体: 无线局域网, 数字卫星通道等

具体描述:

- 双绞铜线: 最便宜, 最常用, 一直用于电话网. 传输速率取决于线的粗细和传输方和接收方的距离
- 同轴电缆: 俩个同轴的导体, 在电视中应用广泛, 一个发送方可对应多个接收方
- 光纤: 传输速率极高, 不受电磁干扰, 成本高, 一般用于远距离传输(跨海链路)
- 陆地无线电通道: 不需安装物理线路, 可穿墙, 受传播环境影响(障碍物, 干扰对象等)
- 卫星无线电通道: 分为同步卫星, 近地轨道卫星, 需要地面站(与卫星相连)

### 网络核心

![1693405785945](/img/计算机网络与因特网/1693405785945.png)

#### 分组交换

将数据分为较小的数据块, 称为**分组(packet)**. 每个分组通过通信链路和分组交换机(路由器, 链路层交换机)传送, 在两个路由器(或交换机)之间的传输时延:

$$
one-hop\ transmission\ delay(sec) = \frac{L(bit)}{R(bit/s)}
$$

**存储转发**

交换机能够向链路传输该分组之前必须接收到整个分组(即先存储), 当全部收到后才开始向链路输出(即转发)该分组

那么在具有$N$条链路的一条路径上, 端到端的时延为:

$$
d_{host\ to\ host}=N\frac{L}{R}
$$

**排队时延和分组丢失**

每一台分组交换机有许多链路与之相连, 对于每一条链路都有一个输出缓存(output buffer, 也叫做输出队列(output queue)). 到达的数据需要先进入队列排队, 这就会产生**排队时延(queuing delay)**, 排队时延取决于当前网络中的拥塞程度. 当当前队列充满后, 后续到达的数据将被丢弃, 也就是**分组丢失(丢包)(packet loss)**.

**转发表和路由选择协议**

每一台路由器都有一个**转发表**, 用于将目的地址映射到输出链路, 也就是为分组选择接下来的链路, 通过一个个路由器的指引, 分组将最终到达目的地址.

**路由选择协议**用于自动的设置转发表.

#### 电路交换

网络在端到端发送数据之前, 先建立一条连接(这是专门为此次发送预留的), 从发送方到接收方的路径上的交换机将为该连接维护连接状态.

![1693839815949](/img/计算机网络与因特网/1693839815949.png)

**电路交换网络中的复用**

- 频分复用(FDM): 将链路的频谱分段, 每一条连接专用一个频段.
- 时分复用(TDM): 时间被分为固定时间段长度的帧, 每一帧又分为多个时隙, 一条连接使用其中的一个时隙传输数据.

![1693839837389](/img/计算机网络与因特网/1693839837389.png)

#### 网络的网络

![1693723538668](/img/计算机网络与因特网/1693723538668.png)

- ISP(互联网服务提供商)
- IXP(互联网交换点)
- 位于相同等级层次的ISP可以**对等**, 即直接把网络连接到一起而不经过上游的中间ISP. 对等的ISP不进行结算(向另一个ISP付费)
- 多个ISP可以在IXP处对等(或者叫互联)
- 内容提供商需要向第一层ISP付费, 另外也建立一些直接连接到较低层ISP/IXP, 后者是不需要向顶层ISP结算的(因为没有经过它们)

> **总结**: 如今的互联网由10多个顶层 ISP 和数十万的较低层 ISP 构成. 较低层的 ISP 与较高层的 ISP 相连, 较高层 ISP 彼此互联. 用户和内容提供商是较低层 ISP 的客户, 较低层 ISP 是较高层 ISP 的客户. 近年来，主要的内容提供商也已经创建自己的网络, 直接在可能的地方与较低层 ISP 互联.

## 分组交换相关概念

### 时延

![1693725180489](/img/计算机网络与因特网/1693725180489.png)

- 处理时延: 检查分组头部并决定该分组导向何处所需要的时间, 也可以包括纠错等时间. 高速路由器处理时延在微妙或更低的数量级
- 排队时延: 在链路上等待传输的时间. 排队时延可以是毫秒到微秒量级
- 传输时延: 将所有分组的比特**向**链路传输所需要的时间, 取决于分组长度与链路传输速率. 传输时延通常在毫秒到微秒量级
- 传播时延: 分组**在**链路中传播的时间, 这取决于媒介与链路长度. 在广域网中, 传播时延为毫秒量级
- 节点总时延: 以上四个时延的和. 对于不同的链路, 上述四个时延所占比重可能有所不同

### 排队时延, 丢包

令 $a (pkt/s)$ 表示分组到达队列的平均速率, $R (b/s)$ 代表链路传输速率, 假定所有分组长度都为 $L$ 比特, 同时节点缓冲队列无限大. 则**流量强度**为 $La/R$, 当流量强度大于1时, 说明分组到达速率大于分组离开速率, 缓冲队列将会无限延长. 所以, 流量强度不能大于1!

- $La/R \to 0$ 时, 排队时延很小
- $La/R \to 1$ 时, 排队时延很大
- $La/R > 1$ 时, 排队时延将会趋于 $\infty$

当流量强度小于等于1时, 分组到达方式如果是周期性的(即每$L/R$秒到达一个分组)就不会有排队时延, 如果是突发性的(某一个时间同时到达很多分组)也会产生很大的排队时延. 平均排队时延与流量强度的关系如下图:
![1693726357061](/img/计算机网络与因特网/1693726357061.png)

实际中缓冲队列能够容纳的分组是有限的, 超出容量的分组将被丢弃, 也就是丢包.

### 端到端时延

从源到目的地的总时延, 假设源主机到目的主机之间有 $N-1$ 台路由器, 忽略排队时延:

$$
d_{end-end} = N(d_{proc} + d_{trans} + d_{prop})
$$

### 吞吐量

吞吐量: **端到端**吞吐量, 如果对于仅有一个链路一对客户-服务器的网络, 数据到达速率为$R_s$, 转发速率为$R_c$, 则吞吐量为$min(R_s, R_c)$

瞬时吞吐量: 当前时刻主机接收到一个文件的速率

平均吞吐量: 整个文件大小为$F$比特, 传输时间为$T$秒, 则平均吞吐量为$F/T\ bps$

瓶颈链路传输速率: 是网络中限制吞吐量的链路的传输速率, 如下图中的吞吐量是 $min(R_c, R_s, R/10)$, 若$R/10 < R_c or R_s$ 那么瓶颈链路就是图中所示的$R$:

![1693840264749](/img/计算机网络与因特网/1693840264749.png)

## 协议层次与服务模型

### 分层体系结构

#### 协议分层

互联网模型:
![1693733885118](/img/计算机网络与因特网/1693733885118.png)

一些概念:

- 报文(message): 应用层的信息分组
- 报文段(segment): 运输层的分组
- 数据报(datagram): 网络层的分组
- 帧(frame): 链路层的分组

另一种模型:
![1693734282957](/img/计算机网络与因特网/1693734282957.png)

### 封装

应用层数据经过层层封装, 自上到下传输

![1693734522902](/img/计算机网络与因特网/1693734522902.png)

## 互联网攻击

- 病毒 (virus) 是一种需要某种形式的用户交互来感染用户设备的恶意软件
- 蠕虫 (worm) 是一种无须任何明显用户交互就能进入设备的恶意软件
- 拒绝服务攻击 (Denial-of- Service (DoS) attack) 使得网络, 主机或其他基础设施部分不能由合法用户使用
- 分布式 DoS (Distributed DoS, DDoS) 中，攻击者控制多个源并让每个源向目标猛烈发送流量, 造成目标服务器瘫痪
- IP 哄骗 (IP spoofing) 将具有虚假源地址的分组注入因特网, 使一个用户可以冒充另一个用户, 采用端点鉴别解决这个问题
